---
import Graph from '../components//graph/Graph.svelte';
import MainLayout from '../layouts/MainLayout.astro';

import { graph } from '../store.js';

let obj_graph = graph.get();

console.log( "First: ", obj_graph.first );

---

<MainLayout>
    <Graph client:load />
</MainLayout>
